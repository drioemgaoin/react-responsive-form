import React from 'react';
import ReactDOM from 'react-dom';

import ResponsiveForm from './react-responsive-form';

ReactDOM.render(React.createElement(ResponsiveForm), document.getElementById('main'));
